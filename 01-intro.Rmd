# Introdução {#intro}

```{r setup}
knitr::opts_chunk$set(echo = FALSE,
                      message = FALSE,
                      error = FALSE,
                      warning = FALSE)
library(tidyverse)
library(knitr)
library(kableExtra)
```

```{r functions}
tabela_geral <- function(dados, titulo, negrito = TRUE, fontsize = 16, colunas = colnames(dados)){
  
  colnames(dados) <- str_to_upper(colunas)
  
  dados %>% 
    kable(caption = titulo, 
          # escape = FALSE,   # considera o <br> uma quebra de linha, 
          align = c("l", rep("r", ncol(dados)-1)),
          format.args = list(big.mark = ".")) %>% 
    kable_styling(bootstrap_options = c("striped", "hover", "responsive"), 
                full_width = F,
                font_size = fontsize) %>% 
  row_spec(nrow(dados), bold = negrito) %>% 
  row_spec(0, align = "c", color = "#84898C", # pantone monument
           font_size = fontsize - 2, extra_css = 'vertical-align: middle')
  
}
```

Esse é um capítulo de introdução.
